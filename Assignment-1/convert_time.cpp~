#include <iostream>

using namespace std;

int main () 
{
  double seconds_decimal;
  cout << "Enter a time in seconds: ";
  cin >> seconds;
  int seconds = round(seconds_decimal);
  cout << round(seconds_decimal) << " seconds \n";
}
