












/*
 * Validate User
 * 
 * if (UserList.include(username) // UserList is a .txt file loaded in when the server is started.
 * {
 * check password
 * if good, good
 * if bad, send error and break.
 * }
 * else
 * {
 * UserModel::makeNewUser();
 * }
 * 
 * Send sheet to user
 *
 */
#include <iostream>
#include "spreadsheet_controller.h"
